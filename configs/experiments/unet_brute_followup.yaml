# @package _global_

runs:
  - unet_brute_wide_aux5
  - unet_brute_xlarge

n_epochs: 30
early_stop_patience: 15

data:
  num_workers: 6
  prefetch_factor: 4

wandb:
  enabled: true
  project: ci2026
  entity: ${oc.env:WANDB_ENTITY,null}
  mode: online
