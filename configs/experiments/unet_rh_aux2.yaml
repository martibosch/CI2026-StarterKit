# @package _global_

defaults:
  - override /model: unet

exp_name: unet_rh_aux2
learning_rate: 0.0005

network:
  use_rh: true
  input_dim: 37
  n_auxiliary_fields: 2
  normalisation_path: data/stats/normalization_rh_aux2.json
