# @package _global_

defaults:
  - override /model: mlp

exp_name: baseline_mlp
learning_rate: 0.001
n_epochs: 50
early_stop_patience: null

data:
  num_workers: 6
  prefetch_factor: 4

wandb:
  enabled: true
  project: ci2026
  entity: ${oc.env:WANDB_ENTITY,null}
  mode: online
