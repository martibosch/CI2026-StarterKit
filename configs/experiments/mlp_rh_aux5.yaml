# @package _global_

defaults:
  - override /model: mlp

exp_name: mlp_rh_aux5
learning_rate: 0.001

network:
  use_rh: true
  input_dim: 40
  n_auxiliary_fields: 5
  normalisation_path: data/stats/normalization_rh_aux5.json
