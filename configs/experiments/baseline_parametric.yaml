# @package _global_

defaults:
  - override /model: parametric

exp_name: baseline_parametric
learning_rate: 0.01
n_epochs: 50
early_stop_patience: null

data:
  num_workers: 6
  prefetch_factor: 4

wandb:
  enabled: true
  project: ci2026
  entity: ${oc.env:WANDB_ENTITY,null}
  mode: online
