# @package _global_

defaults:
  - override /model: unet

exp_name: unet_rh_aux5
learning_rate: 0.0005

network:
  use_rh: true
  input_dim: 40
  n_auxiliary_fields: 5
  normalisation_path: data/stats/normalization_rh_aux5.json
