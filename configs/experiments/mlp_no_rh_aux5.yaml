# @package _global_

defaults:
  - override /model: mlp

exp_name: mlp_no_rh_aux5
learning_rate: 0.001

network:
  use_rh: false
  input_dim: 33
  n_auxiliary_fields: 5
  normalisation_path: data/stats/normalization_no_rh_aux5.json
