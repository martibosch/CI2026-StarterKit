# @package _global_

runs:
  - geounet
  - geounet_wide
  - geounet_hilr

n_epochs: 40
early_stop_patience: null

data:
  num_workers: 6
  prefetch_factor: 4

wandb:
  enabled: true
  project: ci2026
  entity: ${oc.env:WANDB_ENTITY,null}
  mode: online
