# @package _global_

defaults:
  - override /model: unet

exp_name: unet_no_rh
learning_rate: 0.0005

network:
  use_rh: false
  input_dim: 30
  n_auxiliary_fields: 2
  normalisation_path: data/stats/normalization_no_rh_aux2.json
