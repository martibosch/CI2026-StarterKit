defaults:
  - data: default
  - model: mlp
  - hydra: default
  - _self_

exp_name: baseline_mlp
store_path: data/models/${exp_name}
device: cpu
seed: 42
n_epochs: 10
batch_size: 64
learning_rate: 0.001
LR_patience: 7
LR_factor: 0.25

wandb:
  enabled: false
  project: ci2026
  entity: null
  mode: online
